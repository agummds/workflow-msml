name: ml-project
conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_file: {type: string}
      model_path: {type: string}
    command: "python modelling.py {data_file} {model_path}" 